import React, { Component } from "react";

class PurchaseDetail extends Component {
    render() {
        const { className } = this.props;

        <div className={`${className} PurchaseDetail`}>
            Purchase detail goes here
        </div>
    }
}

export default PurchaseDetail;